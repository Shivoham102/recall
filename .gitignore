.env
.env.local
.env.*.local
__pycache__/
*.pyc
*.pyo
backend/.venv/
.venv/
venv/
dist/
build/
*.egg-info/
node_modules/
app/dist/
app/src-tauri/target/
app/src-tauri/gen/
.DS_Store
Thumbs.db

# Google OAuth credentials — never commit these
backend/credentials.json
backend/token.json
backend/last_checkin.json

# Cursor / Claude local settings
.claude/settings.local.json
.cursor/

# Local env overrides
app/.env.local
app/.env

# Cartesia CLI state
.cartesia/

# PyInstaller build output
backend/dist/
backend/build/
backend/*.spec.bak

# Compiled sidecar binaries — built by CI, not committed (except .gitkeep)
app/src-tauri/binaries/*
!app/src-tauri/binaries/.gitkeep
